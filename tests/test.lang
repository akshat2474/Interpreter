// Test all features
print "=== Testing Interpreter ===";

// Variables
let x = 10;
let y = 20;
print "x + y = " + (x + y);

// Functions
function greet(name) {
    return "Hello, " + name;
}
print greet("Student");

// Recursion
function fib(n) {
    if (n <= 1) return n;
    return fib(n - 1) + fib(n - 2);
}
print "Fib(8) = " + fib(8);

// Loops
for (let i = 1; i <= 3; i = i + 1) {
    print "Loop: " + i;
}

print "=== All Tests Passed! ===";
